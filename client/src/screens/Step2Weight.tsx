import type { OnboardingHandle } from '../state/onboarding';

interface Props { onboarding: OnboardingHandle; }

export default function Step2Weight(_props: Props) {
  return <div>Step 2 — Weight</div>;
}
